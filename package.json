{
  "name": "ticobot",
  "version": "0.1.0",
  "description": "Intelligent platform for analyzing Costa Rica's 2026 Government Plans",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter backend --filter frontend dev",
    "build": "pnpm --recursive build",
    "build:check": "pnpm --filter @ticobot/shared build && pnpm --filter @ticobot/backend build && pnpm --filter @ticobot/frontend build",
    "test": "pnpm --recursive test",
    "lint": "echo '⚠️  Linting skipped (ESLint config needed)' && exit 0",
    "type-check": "pnpm --filter @ticobot/shared build && pnpm --filter @ticobot/backend type-check && pnpm --filter @ticobot/frontend type-check",
    "validate": "pnpm type-check && pnpm build:check",
    "pre-deploy": "pnpm validate",
    "clean": "pnpm --recursive clean"
  },
  "keywords": [
    "rag",
    "llm",
    "costa-rica",
    "government-plans"
  ],
  "author": "juanpredev",
  "license": "MIT",
  "packageManager": "pnpm@10.22.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}
