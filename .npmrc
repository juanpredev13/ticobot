# pnpm configuration
auto-install-peers=true
shamefully-hoist=false
strict-peer-dependencies=false

# Disable frozen lockfile for Railway deployments
# Railway will use --frozen-lockfile by default in CI, but we need flexibility
frozen-lockfile=false
